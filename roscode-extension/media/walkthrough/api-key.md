# Anthropic API key

roscode runs on Claude Opus 4.7 — Anthropic's flagship model.

You can set your key in two ways:

- **Settings** → search `roscode.anthropicApiKey`
- **Environment variable** `ANTHROPIC_API_KEY` (shell / launchd)

Your key stays local. roscode talks directly to the Anthropic API — no middleman.

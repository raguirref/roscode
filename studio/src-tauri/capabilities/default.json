{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the main window: Tauri core IPC + shell plugin for limactl.",
  "windows": ["main"],
  "permissions": ["core:default", "shell:default"]
}
